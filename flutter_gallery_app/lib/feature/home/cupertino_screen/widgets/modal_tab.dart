import 'package:flutter/material.dart';

class ModalTab extends StatelessWidget {
  const ModalTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Modal Tab'),
    );
  }
}
